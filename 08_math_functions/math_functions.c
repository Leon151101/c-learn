#include <stdio.h>
#include <math.h>

int main() {
    double x = 3.14;
    x = sqrt(x);
    //x = pow(x, 2);
    //x = round(x);
    //x = ceil(x);
    //x = floor(x);
    //x = abs(x); 
    //x = log(x); // ln 
    //x = log10(x);
    //x = sin(x);
    //x = cos(x);
    //x = tan(x);

    printf("%lf", x);

    return 0;
}