#include <stdio.h>

int main() {
    int x = 10;
    int y = 3;
    double z;
   
    z = x + y;
    //z = x - y;
    //z = x * y;
    //z = x / y;
    //z = x / (double)y;
    //z = x % y;
    //x++;
    //x--;
    //x += 2;
    //x -= 2;
    //x *= 2;
    //x /= 2;
    //x %= 2;


    printf("Result: %.2lf", z);

    return 0;
}