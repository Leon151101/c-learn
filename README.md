# How to compile and link a file to an executable

    ```bash
    gcc <relative/path/to/file.c> -o build/<program_name>

This will create an executable file in the build directory.
It can be run via the terminal:

    ```bash
    build/<program_name>